---
import Layout from "../layouts/Layout.astro";
interface Props {
  date: string;
  title: string;
  description: string;
}

const { date, title, description } = Astro.props;
---

<div class="text-center p-4">timeline</div>
